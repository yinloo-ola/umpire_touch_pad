# Plan 2.1 Summary

## Tasks Completed
- Created `00007_add_match_remarks.sql` goose migration.
- Added remarks column to `GetMatch`, `GetIncompleteMatchesForPeriod`, and `GetAllMatches` queries in `query.sql`.
- Created `AdminUpdateMatch` and `DeleteGamesForMatch` queries in `query.sql`.
- Added `AdminMatchUpdateRequest` struct and `AdminUpdateMatch` method in `match_svc.go` with score validation and sequential game re-indexing.
- Created `handleAdminUpdateMatch` in `handlers.go` and exposed it at `PUT /api/admin/matches/{id}`.

## Verification
- Verified migration adds `remarks` column.
- Verified service processes updates and validates scores.
- Endpoint protected and correctly registered.
